import type { Node, Edge } from '@xyflow/react';
import { defaultConfigs } from '../types/node-types';

// Blank canvas — truly empty start for new workflows
export const initialNodes: Node[] = [];

export const initialEdges: Edge[] = [];
